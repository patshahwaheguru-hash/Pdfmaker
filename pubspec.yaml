name: excel_image_pdf
description: Excel and Image to PDF Converter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'  # <-- ADD THIS LINE. THIS IS THE FIX

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.10.7
  printing: ^5.12.0
  file_picker: ^6.1.1
  excel: 3.0.0  # keep it downgraded
  path_provider: ^2.1.3

flutter:
  uses-material-design: true
